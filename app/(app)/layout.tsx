// Prompt 3+: Protected shell layout (sidebar nav, session guard)
// Authentication enforcement and nav chrome go here.
export default function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
