// Prompt 14: Module player page (video + transcript + progress tracking)
export default function LearnModulePage({
  params,
}: {
  params: { moduleId: string };
}) {
  return <div>Module {params.moduleId} — scaffold placeholder</div>;
}
